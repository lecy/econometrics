---
title: "Weighted Composite Indices"
subtitle: "Lecture note for p-06, in more detail"
eyebrow: "Research Design"
dek: "A more detailed note on measurement theory — why an index that weights each item by its quality measures a latent construct more reliably than a simple average."
concepts_sub: "From simple averages to weighted composites, in plain language."
relationships_sub: "The one-factor model, the optimal weights, and a worked example."
clear_sub: "The short version, and how it connects to factor analysis."
---

<div class="tip">

KEY CONCEPTS:

### Start from the measurement model.

[Signal, Noise, and Reliable Indices](p-06.01-measurement-theory-notes.html) introduced the
basic model. Every observed measure of a latent construct is a true score plus error:

`M = T + e`

and reliability is the share of the observed variance that is signal:

`reliability = var(T) / var(T + e)`

An instrument combines several items so the errors partly cancel. The question for this note
is **how** to combine them.

### A simple index treats every item as equally good.

The usual approach adds or averages the items:

`M = X1 + X2 + X3`  or  `M = (X1 + X2 + X3) / 3`

Summing and averaging have the same reliability, because dividing by 3 rescales the signal and
the noise by the same amount. Both give every item the same weight.

### But items are not equally good.

Each item has a part that tracks the true construct and a part that is noise:

`X1 = T1 + e1`

The strength of the link between `T1`, `T2`, `T3` and the true construct `T` varies from item to
item. A simple average gives an item that barely tracks `T` the same influence as an item that
tracks it closely:

`M_avg = (1/3)(T1 + e1) + (1/3)(T2 + e2) + (1/3)(T3 + e3)`

### A weighted composite gives better items more influence.

`M_w = w1(T1 + e1) + w2(T2 + e2) + w3(T3 + e3)`

The weight on each item should move **inversely with its noise**. If `X1` has a weak
relationship with `T`, then `T1` is small, `e1` is large, and `w1` should be small relative to
the weights on the stronger items.

Chosen this way, the weighted composite has **less measurement error and higher reliability**
than the simple average, and it sits closer to the true score `T`. Factor analysis is the
standard way to estimate the weights, because it estimates how strongly each item loads on
the construct and how much of each item is unique noise.

</div>

# The math

## 1. The one-factor model

Assume a single latent trait `T` measured by three items:

`X_i = λ_i T + e_i`,  for i = 1, 2, 3

`λ_i` is the item's **loading**, its signal strength. `e_i` is its error, with variance
`σ²_ei`. Collect the loadings in a vector `λ`, the weights in a vector `w`, and the error
variances and covariances in a matrix `Σ_e`.

A linear composite is:

`M_w = w1 X1 + w2 X2 + w3 X3 = (w′λ) T + w′e`

Fix the scale by requiring `w′λ = 1`. Then the composite is the true score plus a weighted sum
of the errors:

`M_w = T + w′e`

and its **measurement error variance** is:

`Var(M_w − T) = w′ Σ_e w`

## 2. The optimal weights down-weight noisy items

Choose `w` to minimize the error variance `w′ Σ_e w`, subject to `w′λ = 1`. The solution is:

`w* ∝ Σ_e⁻¹ λ`,  scaled so that `w*′λ = 1`

When the errors are independent, `Σ_e` is diagonal and each weight has a simple form:

`w*_i ∝ λ_i / σ²_ei`

**High-loading, low-error items get higher weights.** Equal weights are optimal only when
`λ_i / σ²_ei` is the same for every item, for example when all the items have identical
loadings and identical error variances.

## 3. A worked example

Three items with these loadings and error variances:

| Item | Loading `λ` | Error variance `σ²_e` | `λ / σ²_e` | Optimal weight | Equal weight |
|---|---|---|---|---|---|
| X1 | 0.9 | 0.20 | 4.50 | 0.750 | 0.500 |
| X2 | 0.7 | 0.30 | 2.33 | 0.389 | 0.500 |
| X3 | 0.4 | 0.50 | 0.80 | 0.133 | 0.500 |

Both sets of weights are scaled so that `w′λ = 1`. For equal weights that means `w = 1 / (0.9 +
0.7 + 0.4) = 0.5` each.

* **Optimal weights:** error variance = 0.75²(0.20) + 0.389²(0.30) + 0.133²(0.50) ≈ **0.167**
* **Equal weights:** error variance = 0.5²(0.20 + 0.30 + 0.50) = **0.250**

The weighted composite **cuts the error variance by about a third.** Nearly all of the gain
comes from shrinking the weight on X3, the weakest and noisiest item.

## 4. Reliability

The reliability of a composite is its signal variance over its total variance:

`α_w = σ²_T (w′λ)² / [ σ²_T (w′λ)² + w′ Σ_e w ]`

With the constraint `w′λ = 1`, the numerator is fixed at `σ²_T`, so **maximizing reliability is
the same as minimizing error variance.** The optimal weights maximize reliability, and equal
weights reach the same reliability only when the items are equally informative.

In the example, with `σ²_T = 1`:

* **Optimal weights:** reliability = 1 / (1 + 0.167) ≈ **0.86**
* **Equal weights:** reliability = 1 / (1 + 0.250) = **0.80**

## 5. How this connects to Cronbach's alpha and factor analysis

**Cronbach's alpha** is the most common reliability statistic for an index, and it measures
**internal consistency**. It is calculated for the *unweighted* sum of the items and assumes
every item measures the construct equally well. When loadings differ, alpha understates the
reliability that a well-weighted composite can achieve. McDonald's omega relaxes the
equal-loadings assumption, and coefficient H gives the reliability of the optimally weighted
composite itself.

**Factor analysis** estimates exactly the ingredients the optimal weights need: the loadings
`λ` and the unique variances `σ²_e`. Bartlett factor scores use the weights `Σ_e⁻¹λ` directly,
and regression factor scores are proportional to them when there is a single factor.

**Principal component analysis** also produces a weighted composite, but its weights are chosen
to capture total variance. It doesn't separate signal from noise, so its weights only
approximate the reliability-maximizing ones. See the
[PCA versus Factor Analysis](measurement-pca-vs-factor-analysis.html) walkthrough.

# What should be clear in my mind?

## The short version

* Each item = signal (`λ_i T`) + noise (`e_i`).
* A simple average assumes every item is equally good.
* The optimal weights are `w_i ∝ λ_i / σ²_ei`: strong, clean items count more.
* These weights minimize error variance and maximize reliability.
* Equal weights are optimal only when every item is equally informative.

## How much does weighting matter in practice?

It depends on how different the items are. In the
[Factor Analysis](measurement-factor-analysis.html) walkthrough, six strong socioeconomic items
already form a highly reliable simple index (α = 0.94), and the simple average correlates 0.96
with the weighted factor score. When items are uniformly strong, weighting changes little. When
one or two items are much noisier than the rest, as in the worked example above, weighting
pays off.

## Questions to ask of any index

* Are the items combined with equal weights, or weighted by how well each measures the
  construct?
* Is there reason to think some items are much noisier than others?
* Is the reported reliability Cronbach's alpha, which assumes equal loadings, or a coefficient
  that allows them to differ?
* If the weights came from PCA, would factor analysis give a different answer?
